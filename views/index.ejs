<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>all posts</title>
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <h1>Quora Posts</h3>
    <% for(let post of posts){%>
    <div class="posts">
        <h3 class="user">@<%= post.username %></h3>
        <h4 class="content"><%= post.content %></h4>
        <a class="btn" href="http://localhost:8080/posts/<%= post.id  %>"> Details</a>
        <a  class="btn" href="http://localhost:8080/posts/<%= post.id  %>/edit"> Edit Post</a> 
        <form style="display:inline" method="post" action="/posts/<%= post.id %>?_method=DELETE">
            <button >Delete Post</button>
        </form>
    </div>
    <% } %>

    <br>
    <buttom> <a class="btn"  href="http://localhost:8080/posts/new">Create New Post</a> </buttom>
</body>
</html>